# ============================================================
# requirements.txt  –  Python 3.11+ dependencies
# ============================================================

# HTTP / networking
requests>=2.31.0
aiohttp>=3.9.0

# XML / HTML parsing
lxml>=5.1.0
beautifulsoup4>=4.12.0

# PDF extraction
pdfplumber>=0.10.3
pypdf>=4.0.0

# Hugging Face
huggingface_hub>=0.21.0
datasets>=2.17.0
pyarrow>=15.0.0

# Semantic similarity / deduplication
sentence-transformers>=2.6.0
numpy>=1.26.0
scikit-learn>=1.4.0

# Retry / resilience
tenacity>=8.2.3

# Progress & CLI
tqdm>=4.66.0

# Environment / config
python-dotenv>=1.0.0
PyYAML>=6.0.1

# Database (lightweight indexing)
# sqlite3 is bundled with Python — no extra package needed

# Utilities
uuid  # bundled with Python
